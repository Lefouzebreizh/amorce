# Appels à l'API Graph de Facebook.
requests>=2.31

# Lecture de config.env. Les identifiants ne sont ni dans le code, ni dans
# l'historique : ils vivent dans un fichier ignoré par git.
python-dotenv>=1.0

# Rédaction des réponses. L'alerte de fin passe par `smtplib`, de la
# bibliothèque standard : pas de dépendance pour dix lignes de courriel.
anthropic>=0.40
