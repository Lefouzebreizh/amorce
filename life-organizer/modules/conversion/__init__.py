"""Module 4 — conversion de format et compression sans perte visible.

Trois fichiers à écrire, dans cet ordre :

- `regles.py` — choisir la règle applicable à un fichier, décider si le gain justifie la conversion (seuil_gain_minimal_pct), composer les arguments de la ligne de commande.
- `traitement.py` — appeler Pillow ou ffmpeg, vérifier le fichier produit avant de retirer l'original.
- `commande.py` — arguments, affichage, code de sortie.

Réglages dans `organizer_config.json`, section « conversion ».
"""
