# Cours des actions et des ETF, et taux de change. Tire pandas derrière lui :
# c'est pourquoi `assistant.py` ne l'importe qu'au moment de sortir sur le
# réseau, et pas en tête de fichier.
yfinance>=0.2.40

# Cours des cryptos, en euros directement (API publique CoinGecko, sans clé).
requests>=2.31

# Tableaux console. Le seul rôle de la bibliothèque est l'alignement : tout le
# formatage des montants est fait à la main, à la française.
tabulate>=0.9
