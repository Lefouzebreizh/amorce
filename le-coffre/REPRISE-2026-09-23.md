# Reprise de Mon Tiroir Secret — 23 septembre 2026

Base relue : `origin/main` au commit `12e1338`. Direction conservée : le coffre
ouvert sur la mer, déjà validé par le propriétaire. Aucun changement du modèle
de chiffrement, des règles RLS, des quotas ou du stockage Supabase.

## Changements du lot

- ajout d'une entrée mobile « Photographier » qui demande l'appareil arrière et
  renvoie la photo dans le parcours de dépôt déjà éprouvé ;
- remise à zéro des entrées de fichier pour pouvoir reprendre immédiatement le
  même document après une annulation ou une correction ;
- suppression du vide mesuré entre la scène d'accueil et le formulaire d'accès ;
- nom visible harmonisé en « Mon Tiroir Secret » ;
- explication de l'IA alignée sur le mode privé par défaut ;
- confirmation accessible après l'envoi du lien de récupération ;
- import `pdf-lib` corrigé : le build n'émet plus l'avertissement qui pouvait
  masquer un échec du remplissage de formulaire dans le navigateur ;
- polices servies par Next plutôt que chargées depuis Google au rendu ;
- tests de tri remis en cohérence avec le classement local, sans envoi IA.

## Contrôles

- `npm test` : 128 tests réussis sur 128 ;
- `npm run typecheck` : réussi ;
- `npm run lint` : réussi sans avertissement ;
- build Next.js de production avec configuration Supabase fictive : réussi,
  dix routes statiques générées, aucun avertissement de compilation ;
- `git diff --check` : réussi.

Le site public a été ouvert avant correction et a confirmé la scène, le parcours
d'accès et l'espace vide sous le héros. L'aperçu local corrigé n'est pas
accessible au navigateur de recette (`ERR_BLOCKED_BY_CLIENT`) : le rendu exact
de ce commit doit être ouvert sur un aperçu Vercel avant toute annonce de
publication terminée ou toute fusion dans `main`.
