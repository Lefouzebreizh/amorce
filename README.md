# amorce

> **Amorce** *(n.f.)* — ce par quoi une chose commence ; point de départ.

Dépôt de démarrage. Le projet vient d'être initialisé : ce fichier est pour
l'instant le seul contenu versionné.

## Statut

🚧 En cours d'initialisation — le contenu applicatif reste à définir.

## Structure du dépôt

```
.
└── README.md    # ce fichier
```

## Démarrage

```bash
git clone https://github.com/lefouzebreizh/amorce.git
cd amorce
```

Les étapes d'installation et de lancement seront complétées dès que la pile
technique (langage, gestionnaire de paquets, outils) aura été choisie.

## Contribuer

1. Créer une branche à partir de `main` : `git checkout -b ma-branche`
2. Committer les modifications avec un message clair et descriptif
3. Pousser la branche : `git push -u origin ma-branche`
4. Ouvrir une pull request

## Licence

À définir.
