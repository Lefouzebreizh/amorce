# Secrets d'outillage (scripts sous .claude/skills/) — jamais ceux de l'app
# Amorce elle-même, qui vivent dans le .env de la racine (voir .env.example).
#
# Copier ce fichier en .claude/.env (déjà ignoré par git, comme .env* à la
# racine) et y coller la vraie valeur — jamais dans le chat, jamais en
# argument de ligne de commande.

# Clé Google AI Studio (niveau gratuit), pour la vérification de rythme par
# Gemini avant livraison d'un montage. Voir
# .claude/skills/montage-sans-refaire/scripts/verifier_gemini.py
GEMINI_API_KEY=
